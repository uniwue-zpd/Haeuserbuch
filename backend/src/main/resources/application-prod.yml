spring:
  jpa:
    # Production schema is restored/managed outside Hibernate; fail fast on a
    # missing or incompatible schema instead of partially creating one.
    hibernate:
      ddl-auto: validate
